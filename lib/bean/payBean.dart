class PayBean {
  String headUrl;
  String name;
  String count;
  String limit;
  String onlineState;
  String money;

  PayBean(this.headUrl, this.name, this.count, this.limit, this.onlineState,
      this.money);

}